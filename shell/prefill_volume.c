#include <stdio.h>


static struct nexus_volume * mounted_value = NULL;

static int
__create_attributes(size_t count)
{
    return -1;
}

int
prefill_volume_main(int argc, char ** argv)
{
    return -1;
}
