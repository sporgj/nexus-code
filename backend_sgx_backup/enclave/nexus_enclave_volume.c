#include "nexus_trusted.h"

int
nexus_enclave_add_volume(struct supernode * supernode,
                         struct volumekey * volumekey)
{
    return -1;
}

int
nexus_enclave_del_volume(struct nexus_uuid * root_uuid)
{
    return -1;
}

struct volumekey *
nexus_enclave_get_volumekey(struct nexus_uuid * root_uuid)
{
    return NULL;
}
