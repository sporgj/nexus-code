#pragma once

/**
 * File handles external commands passed to a socket
 * @author Judicael Djoko
 */


void
commander_destroy(struct sgx_backend * sgx_backend);

void
commander_destroy(struct sgx_backend * sgx_backend);
