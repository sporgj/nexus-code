syntax = "proto2";

message dnode {

    message fentry {
        required bytes encoded_name = 1;
        required bytes raw_name = 2;
    }

    // all the entries
    repeated fentry file = 1;
    repeated fentry dir = 2;
}
